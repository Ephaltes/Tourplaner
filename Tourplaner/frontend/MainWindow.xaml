<Window x:Class="frontend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:frontend.ViewModels"
        xmlns:navigation="clr-namespace:frontend.Navigation"
        xmlns:views="clr-namespace:frontend.Views"
        xmlns:custom_controls="clr-namespace:frontend.UserControls"
        mc:Ignorable="d"
        Title="MainWindow"
        MinHeight="720"
        MinWidth="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize" AllowsTransparency="True">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"/>
    </WindowChrome.WindowChrome>

    <Grid>
        <!--Background Color-->
        <Grid Background="#192742">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="65" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <!--Close / Minimize / Maximize--> 
            <custom_controls:button_menu_uc Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2"/>

            <!--Navigation-->
            <custom_controls:navigation_uc Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" />

            <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding CurrentViewModel}">
               <ContentControl.Resources>
                   <DataTemplate DataType="{x:Type local:HomeViewModel}">
                       <views:HomeView/>
                   </DataTemplate>
                    <DataTemplate DataType="{x:Type local:SettingsViewModel}">
                        <views:SettingsView/>
                    </DataTemplate>
                </ContentControl.Resources>
            </ContentControl>
            <!-- <Button Content="Switch to TestView"  -->
            <!--         Command="{Binding UpdateCurrentViewModelCommand}"  -->
            <!--         CommandParameter="{x:Static navigation:ViewType.Test}" Margin="64,244,64,18" /> -->
        </Grid>
    </Grid>
</Window>
